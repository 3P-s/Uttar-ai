import React from 'react';

const Prompts = (props) => {
    const handleClick = () => {
        props.handleTagChange({ target: { value: props.tag } });
    };

    return (
        
    );
};

export default Prompts;